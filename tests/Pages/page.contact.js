// Vendors
import React, { useState, memo } from 'react'
import styled from 'styled-components'
// Components

// Main Component
const Contact = (props) => {
  // State
  const [randomState, setRandomState] = useState('Contact')

  // Local vars

  // Effects

  // Functions
  const randomFunction = async () => {}

  // Template
  return <Wrapper>{randomState}</Wrapper>
}

// Style
const Wrapper = styled.div``

// Exports
export default memo(Contact)
